anthropic==0.68.0
replicate==1.0.3
httpx==0.27.2
# HDR bracket merge: rawpy decodes Sony ARW (and other RAW formats)
# via the bundled LibRaw. exifread reads capture timestamp +
# exposure bias from RAW files for bracket-set grouping.
rawpy==0.21.0
exifread==3.0.0
