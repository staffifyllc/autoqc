{
  "_crons_paused": "ALL VERCEL CRONS PAUSED 2026-05-31. At $0 revenue, every automated loop costs more than it returns. To re-enable: move the entries back into a 'crons' array. Each path is preserved here so you don't have to look them up.",
  "_disabled_dropbox_autohdr": {
    "path": "/api/cron/dropbox-autohdr",
    "schedule": "*/30 * * * *",
    "reason": "Was the smoking gun for the May 27-30 S3 egress spike. Re-enable only after pushCompletedProperties is idempotent (mark dropboxPushedAt per-photo, not per-property)."
  },
  "_disabled_stuck_pending": {
    "path": "/api/cron/stuck-pending-recovery",
    "schedule": "0 14 * * *"
  },
  "_disabled_bug_triage": {
    "path": "/api/cron/bug-triage",
    "schedule": "0 12 * * *"
  },
  "_disabled_staffify_sync": {
    "path": "/api/cron/sync-staffify-clients",
    "schedule": "17 * * * *"
  }
}
